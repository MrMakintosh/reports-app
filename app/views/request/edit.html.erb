<div class="form">
  <%= form_for @request do |r| %>
    <div class="field">
      <%= r.label 'Отчет администратора' %><br/>
      <%= r.text_area :admin_message %>
    </div>
    <div class="input-button">
      <%= r.submit 'Выполнено' %>
    </div>
  <% end %>
</div>